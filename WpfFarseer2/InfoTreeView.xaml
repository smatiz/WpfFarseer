<UserControl x:Class="WpfFarseer.InfoTreeView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:sm="sm:farseer"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <Grid>
        <TreeView ItemsSource="{Binding UserControlChildren}" Grid.Column="0">
            <TreeView.Resources>
                <!--<HierarchicalDataTemplate  DataType="{x:Type sm}" ItemsSource="{Binding Children}">
                    <TextBlock Foreground="Red" Text="{Binding Name}"></TextBlock>
                </HierarchicalDataTemplate>
                <HierarchicalDataTemplate  DataType="{x:Type sm:Leaf}" >
                    <TextBlock Text="{Binding Path=Name}"></TextBlock>
                </HierarchicalDataTemplate>-->
            </TreeView.Resources>
        </TreeView>
    </Grid>
</UserControl>
